from setuptools import setup, find_packages
import glob

setup(
    name='det2d',
    version='0.0.1',
    packages=find_packages(),
    description=("2D object detection ops and models"),
    author='Boshu',
    keywords='computer vision, 2D object detection'
)